<!DOCTYPE html>
<html lang="en">

<%- include('../../layout/head')%>

    <body>
        <section>
            <img src=<%=item.image_url %> alt= <%= item.title %>>
                <h5>
                    <%= item.title %>
                </h5>
                <h4 class="">
                    <%= item.author %>
                </h4>
                <p>
                    <%= item.description %>
                </p>
                <h3>
                    isbn: <%= item.isbn %>
                </h3>
        </section>
    </body>

</html>